name: Email Test Action

on:
  push:    # Trigger when you push to the repo
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 23f2003826@ds.study.iitm.ac.in
        run: echo "Hello, world! This is my GitHub Action test run."
